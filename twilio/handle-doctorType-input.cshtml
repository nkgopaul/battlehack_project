@{
    Response.ContentType = "text/xml";
    
    var twiml = new Twilio.TwiML.TwilioResponse();
    
    int user_pushed = int.Parse(Request["Digits"]);
 
    if (user_pushed == 1) {
        twiml.Say("You have chosen dermatologist.");
        //store condition
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/severity.xml", "GET");
    }
    else if (user_pushed == 2) {
        twiml.Say("You have chosen orthopedic.");
        //store condition
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/severity.xml", "GET");
    }
    else if (user_pushed == 3) {
        twiml.Say("You have chosen chiropractor.");
        //store condition
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/severity.xml", "GET");
    }
    else if (user_pushed == 4) {
        twiml.Say("You have chosen cardiologist.");
        //store condition
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/severity.xml", "GET");
    }
    else if (user_pushed == 5) {
        twiml.Say("You have chosen psychiatrist.");
        //store condition
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/severity.xml", "GET");
    }
    else if (user_pushed == 6) {
        twiml.Say("You have chosen endocrinologist.");
        //store condition
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/severity.xml", "GET");
    }
    else if (user_pushed == 7) {
        twiml.Say("You have chosen pediatrician.");
        //store condition
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/severity.xml", "GET");
    }
    else if (user_pushed == 8) {
        twiml.Say("You have chosen general doctor.");
        //store condition
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/severity.xml", "GET");
    }
    else {
        twiml.Say("Sorry, I didn't get your response. Let's try again.");
        twiml.Redirect("https://raw.githubusercontent.com/nkgopaul/battlehack_project/master/twilio/doctorType.xml", "GET");
    }
}